{
  "name": "voicebridge",
  "version": "0.2.0",
  "description": "Hands-free, two-way voice for coding agents (Claude Code, Codex, Antigravity) from your phone. Free, open-source, no ElevenLabs. Browser Web Speech (STT/TTS) + a tiny zero-dependency Node bridge that drives the agent CLI on your machine.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "voicebridge": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "smoke": "bash scripts/smoke.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "optionalDependencies": {
    "web-push": "^3.6.7"
  }
}
